<IfModule mod_rewrite.c>
    RewriteEngine On

    # Only rewrite non-existent files and directories to index.php
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>